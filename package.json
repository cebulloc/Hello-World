{
  "name": "fretforge",
  "private": true,
  "version": "0.1.0",
  "description": "Learn guitar by playing it. Theory taught through the fretboard.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm --workspace server run dev",
    "dev:client": "npm --workspace client run dev",
    "build": "npm --workspace server run build && npm --workspace client run build",
    "db:migrate": "npm --workspace server run db:migrate",
    "db:seed": "npm --workspace server run db:seed",
    "db:reset": "npm --workspace server run db:reset",
    "lint": "npm --workspace server run lint && npm --workspace client run lint",
    "test": "npm --workspace server run test && npm --workspace client run test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
